<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_location_form_inherit" model="ir.ui.view">
        <field name="name">stock.location.form.inherit</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <!-- Seleziona il gruppo "Additional Information" esistente -->
            <xpath expr="//group[@name='additional_info']" position="inside">
                <!-- Aggiungi i nuovi campi con visibilità condizionale -->
                <field name="m_label_type" attrs="{'invisible': [('usage', '!=', 'internal')]}"/>
                <field name="m_storage_type" attrs="{'invisible': [('usage', '!=', 'internal')]}"/>
            </xpath>
        </field>
    </record>
</odoo>
