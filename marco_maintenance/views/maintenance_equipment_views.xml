<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record
    id="maintenance_equipment_view_form_inherit_marco_maintenance"
    model="ir.ui.view">
        <field name="name">maintenance.equipment.view.form.inherit.marco.maintenance</field>
        <field name="model">maintenance.equipment</field>
        <field
        name="inherit_id"
        ref="maintenance.hr_equipment_view_form" />
        <field
        name="arch"
        type="xml">
            <xpath
            expr="//div[@name='button_box']//button[1]"
            position="before">
                <!-- disabled button -->
                <button
                name="action_view_documents"
                type="object"
                class="oe_stat_button disabled"
                icon="fa-file-text"
                attrs="{'invisible':[('documents_count','&gt;',0)]}"
                string="Documents">
                    <field
                    name="documents_count"
                    widget="statinfo_websocket" />
                </button>
                <!-- clickable button -->
                <button
                name="action_view_documents"
                type="object"
                class="oe_stat_button"
                icon="fa-file-text"
                attrs="{'invisible':[('documents_count','&lt;',1)]}"
                string="Documents">
                    <field
                    name="documents_count"
                    widget="statinfo_websocket" />
                </button>
            </xpath>
        </field>
    </record>
</odoo>
